import React from 'react';

/*
    function About(props) 
    --> props 안에는 react-router에 의해서 (라우터 안에 있는 Route들) 기본적으로 내용을 가짐. 
    --> 클릭으로 정보 전달이 가능함. 
*/
function About() {
    return <span>about...</span>
}

export default About;